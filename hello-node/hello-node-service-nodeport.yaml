apiVersion: v1
kind: Service
metadata:
  labels:
    name: hello-node
  name: hello-node
spec:
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30180
  selector:
    name: hello-node
  type: NodePort
