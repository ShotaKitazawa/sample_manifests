---
apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: hello-world-${ trigger.id }
  labels:
    app: hello-world
    tag: ${ trigger.id }
spec:
  replicas: 3
  selector:
    matchLabels:
      app: hello-world
  template:
    metadata:
      labels:
        app: hello-world
        tag: ${ trigger.id }
    spec:
      containers:
      - image: kanatakita/print-tag:${ trigger.id }
        name: hello-world
        ports:
        - containerPort: 8080
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: hello-world-test
spec:
  hosts:
  - "*"
  gateways:
  - hello-world-gateway
  http:
  - match:
    - uri:
        prefix: "/test"
    rewrite:
      uri: "/"
    route:
    - destination:
        host: hello-world.default.svc.cluster.local
        subset: ${ trigger.id }
        port:
          number: 80
---
apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: hello-world-${ trigger.id }
  labels:
    tag: ${ trigger.id }
spec:
  host: hello-world.default.svc.cluster.local
  subsets:
  - name: ${ trigger.id }
    labels:
      tag: ${ trigger.id }
